/**
 * P-007 Configuration — optional ownership placeholder (E-001 bootstrap).
 * Must not be required by core packages.
 */
export const PACKAGE_ID = "P-007" as const;
export const PACKAGE_NAME = "configuration" as const;
