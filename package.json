{
  "name": "flexype-shopify-investigator",
  "version": "0.1.0",
  "private": true,
  "description": "FlexyPe Shopify Store Diagnostics Chrome Extension — release candidate (E-013)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && tsc -p extension/content/tsconfig.json && bash extension/tooling/package-extension.sh",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "architecture:freeze": "bash tooling/check-architecture-freeze.sh",
    "release:structure": "bash tooling/check-release-readiness.sh",
    "release:check": "npm run release:structure && npm run typecheck && npm run lint && npm run format:check && npm test && npm run build"
  },
  "keywords": [
    "shopify",
    "investigation",
    "diagnostics",
    "chrome-extension"
  ],
  "author": "Vinayak Mahindrakar",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^22.13.10",
    "eslint": "^9.22.0",
    "eslint-config-prettier": "^10.1.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.1",
    "vitest": "^3.0.8"
  }
}
